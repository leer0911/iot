import React from 'react';
import { IconButton, Box } from '../../../';
import { Add } from '../../../icon';

const Demo = props => {
  return (
    <Box p={1}>
      <IconButton>
        <Add />
      </IconButton>
    </Box>
  );
};

export default Demo;
