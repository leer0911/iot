import React from 'react';
import { IconButton, Box } from '../../../';

const demo = props => {
  return (
    <Box p={1}>
      <IconButton>121</IconButton>
    </Box>
  );
};

export default demo;
